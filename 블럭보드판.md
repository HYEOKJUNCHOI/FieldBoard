# FieldBoard 웹 편집기 2차 구현 전달서

작성일: 2026-06-02
전달 대상: Sisyphus
상태: 기존 1차 MVP 요구사항 교체 / 새 구현 기준

## 결론

앞전 MD에 있던 1차 내용은 이미 구현된 기준으로 보고 비운다.

이제 구현 목표는 `현장 사진 위에 올라가는 보드판 표를 실제 사용감에 맞게 편집하는 경험`이다. 핵심은 Lucid식 드래그 프리뷰, 휴대폰 실사용 기준 4:3 사진 미리보기, 클릭/입력 분리다.

우선순위는 아래 순서다.

```text
1. 드래그 UX 전체 개선
2. 휴대폰 실사용 기준 4:3 미리보기
3. 버튼/툴바 정리
4. 클릭/입력 분리와 A1식 표기
5. 행 단위 셀 추가/분배
6. 선택/단축키/리사이즈
7. 잠금은 후순위
```

작업 방식은 한 단위씩 `구현 -> 테스트/실사용 검증 -> main 배포 -> 다음 단위`로 진행한다.

## 현재 진행 현황

전체 진행률: 약 83%

완료됨:

- 드래그 UX 전체 개선
  - 선택 셀 자체 드래그
  - 셀 추가 드래그
  - 휴지통/삭제 드롭존 드롭 삭제
  - 드래그 중 공중 카드 분리
  - 원본 행/대상 행 점선·반투명 결과 프리뷰
  - 초록 경계 안 이동, 초록 경계 밖 취소
  - 손을 떼기 전까지 원본 데이터 미변경
  - `선택영역 잡기` 버튼 제거
- 휴대폰 실사용 기준 4:3 미리보기
  - `철근.png` 실제 사진 배경 적용
  - 휴대폰 프레임 안 4:3 사진 영역 구성
  - `4:3 PHOTO` 제거
  - 기본 표 배율 50%
  - `↖`, `↗`, `↙`, `↘` 위치 버튼과 사진 영역 clamp 적용
- 툴바/버튼 1차 정리
  - `NxN` 제거
  - `[] x [] 표 만들기` 유지
  - 정렬/모서리/되돌리기/다시실행 아이콘화
  - 병합/분할/균등 버튼 확대
- 큰 셀 추가/삭제 드롭존
  - 표 바로 위 큰 셀 추가 드롭존
  - 표 바로 위 큰 삭제 드롭존
  - 드래그 중 강조
  - 삭제는 `[data-trash-zone="true"]` 드롭존에 놓을 때만 실행
- 행별 셀 수/폭 기본 동작
  - 삭제/이동 후 해당 행이 빈 칸 없이 자기 셀 수 기준으로 전체 폭 재분배
  - 다른 행 셀 수는 그대로 유지
  - 편집판 행별 셀 수/폭/행 높이가 4:3 미리보기에 즉시 반영
- 클릭/입력 분리와 A1식 표기
  - 1클릭/1탭은 선택만 수행
  - 선택 상태에서 `Enter`로 입력 모드 진입
  - 더블클릭으로 입력 모드 진입
  - `Escape` 또는 blur로 입력 종료
  - 셀 좌표를 `A1`, `B1`, `C1` 방식으로 표시
- 기본 선택/히스토리 단축키
  - `Ctrl + 클릭` 떨어진 셀 부분 선택
  - `Shift + 클릭` 범위 선택
  - `Delete`/`Backspace` 선택 셀 내용 삭제
  - `Ctrl + Z` 되돌리기
  - `Ctrl + Y` 및 `Ctrl + Shift + Z` 다시 실행
- 리사이즈
  - 셀 사이 세로 경계 자유 리사이즈
  - 일반 리사이즈는 해당 행 양옆 셀만 조절
  - `Shift` 리사이즈는 같은 세로 경계가 모든 행에서 포인터 x좌표에 맞춰 일직선 정렬
  - 행 사이 가로 경계로 행 높이 조절
  - 리사이즈 시작 시점만 히스토리 기록
  - `균등` 버튼으로 선택 행/셀 폭 초기화

남음:

- 클릭/입력 분리 후 전체 폰트 그림자 보강
- 셀 안 좌표 중복 표시 완전 정리
- 행 단위 셀 추가의 세부 분배 규칙
  - 삽입 지점 왼쪽 폭 보존
  - 오른쪽/대상 그룹 평균 균등 분배
- 선택/단축키 확장
  - `Shift + 방향키` 선택 확장
  - `Shift + Ctrl + ←` 선택 셀 왼쪽 새 셀 추가
  - `Shift + Ctrl + →` 선택 셀 오른쪽 새 셀 추가
- 색상 UI
  - 배경색/글자색 10개 스와치 드롭다운
  - 사진 위 가독성을 고려한 색상 세트와 글자 그림자 적용
- 빠른 롱프레스 핸들 표시
  - 250~300ms 롱프레스 시 핸들/타겟점 표시
  - 움직임 시작 시 롱프레스 취소
- 모바일/PC `+` 크로스 전체 열/행 조절
- 선택 상태 헤더 최종 정리
  - `A1:B3 선택됨` 같은 범위 표기
  - 상단 툴바/편집 헤더 안쪽으로 더 압축 배치
- 잠금 기능
  - 셀 잠금
  - 잠금 아이콘
  - 잠금 상태에서 자동 재배치 보호

## 1. 실제 4:3 미리보기

현재 미리보기는 실제 카메라 4:3 촬영 영역보다 작게 보인다. 목표는 단순 4:3 카드가 아니라, 모니터에서 보아도 `휴대폰으로 실제 사진을 찍었을 때 보드판이 어느 정도 크기로 보일지` 예측 가능한 화면이다.

구현 기준:

- `4:3 PHOTO` 글자 제거
- 프로젝트 루트의 `철근.png`를 실제 촬영 사진 배경으로 사용
- 바깥은 일반적인 휴대폰 실물 화면 느낌으로 잡고, 그 안에 4:3 촬영/앨범 사진 영역을 크게 표현
- 사진 프레임은 실제 카메라처럼 가로를 거의 꽉 쓰는 촬영 영역이어야 함
- 4:3 사진은 휴대폰에서 찍고 앨범에서 볼 때의 크기감을 기준으로 맞춤
- 표/셀 배율은 미리보기 패널 기준이 아니라 4:3 사진 영역 기준으로 계산
- 표 기본 배율은 우선 `50%`로 시작하되, 휴대폰형 4:3 미리보기 적용 후 실제 화면에서 다시 맞춘다
- 미리보기는 표 편집 영역과 셀 추가/삭제 드롭존에 가깝게 아래쪽으로 재배치
- 배율을 키운 뒤 `↖`, `↗`, `↙`, `↘` 위치 버튼을 누르면 보드판은 사진 영역 밖으로 나가지 않고, 사진 안쪽 최소 여백 위치로 자동 보정되어야 함
- 미리보기 패널/휴대폰 프레임 주변 여백은 조작 버튼과 화면을 구분할 정도만 남기고, 큰 빈 카드 공간처럼 보이면 실패

미리보기 판단 기준:

```text
사진 영역 자체가 커야 함
표 50%는 그 커진 사진 영역 기준이어야 함
사진 주변 여백이 큰 카드처럼 보이면 실패
휴대폰 화면으로 현장 사진을 보는 느낌이 아니라 단순 패널 카드처럼 보이면 실패
배율 변경 후 위치 버튼을 눌렀는데 보드판이 사진 밖으로 나가거나 큰 여백을 남기면 실패
```

## 2. 클릭과 입력 분리

클릭만으로 입력 커서가 잡히면 안 된다.

구현 기준:

- 1클릭/1탭: 셀 선택만
- 클릭만으로 입력 모드 진입 금지
- 선택된 상태에서 `Enter`: 입력 모드
- 더블클릭: 입력 모드
- 입력 모드가 아닐 때는 셀을 잡고 이동할 수 있어야 함
- 셀 표기는 `R1 C1` 방식이 아니라 엑셀처럼 `A1`, `B1`, `C1` 방식으로 표시
- 셀 안에 같은 좌표가 작은 글씨/큰 글씨로 중복 표시되지 않게 정리
- 모든 셀 글자와 주요 UI 글자에는 얕은 그림자를 넣어 사진 위에서도 잘 보이게 함

성공 기준:

```text
셀 클릭 -> 초록 선택 테두리만 표시
Enter -> 텍스트 입력 가능
더블클릭 -> 텍스트 입력 가능
단일 클릭 후 바로 커서가 뜨면 실패
셀 좌표가 R1 C1처럼 보이면 실패
사진 위에서 글자가 배경에 묻히면 실패
```

## 3. Lucid식 드래그 프리뷰

드래그 중 실제 데이터가 먼저 바뀌면 안 된다. 손을 떼기 전에는 결과만 미리 보여줘야 한다.

구현 기준:

- 선택된 셀을 끌면 셀이 공중에 분리된 카드처럼 보임
- `선택 영역 잡기` 버튼에 의존하지 않고, 선택된 셀 자체를 잡아 끌 수 있어야 함
- 원래 행은 그 셀이 빠진 상태를 점선/희미한 프리뷰로 표시
- 예를 들어 4칸짜리 첫 줄에서 1개 셀을 들면, 첫 줄은 남은 3개 셀이 flex/grid처럼 3칸으로 균등하게 재배치된 미리보기로 보여야 함
- 이동할 위치는 놓았을 때 들어갈 결과를 점선/반투명 프리뷰로 표시. 예를 들어 2번째 줄에 꽂으면 2번째 줄은 삽입 슬롯을 포함해 5칸 flex/grid 미리보기로 보여야 함
- 삭제/이동으로 특정 행의 셀 수가 줄면 그 행은 빈 칸을 남기지 않고 남은 셀들이 같은 행 전체 폭을 균등하게 다시 나눠 써야 함
- 예를 들어 맨밑줄 7칸 중 2개를 삭제하면 맨밑줄은 5칸이 전체 줄 너비를 5등분해야 하며, 오른쪽에 2칸 빈 공간이 보이면 실패
- 손을 떼기 전까지 원본 데이터는 변경하지 않음
- 셀이 들린 동안 보드판 전체에는 초록 박스 경계가 보여야 함
- 초록 경계 안이면 삽입/이동 후보
- 초록 경계는 너무 타이트하면 안 되며, 셀 경계/행 경계 근처에서 손이 조금 흔들려도 후보 프리뷰가 깜빡이지 않도록 느슨한 여유 판정을 둬야 함
- 초록 경계 밖에 확실히 놓으면 취소
- 제자리에 놓으면 취소
- 휴지통/삭제 드롭존에 놓을 때만 삭제
- 셀 추가 드래그, 선택 셀 이동 드래그, 휴지통 드롭 삭제, 미리보기 보드판 위치 이동은 같은 조작 톤으로 정리

예시:

```text
현재: [1][2][3][4]
4 위치에 새 셀 또는 이동 셀을 들이댐
원래 행 프리뷰: [ ][ ][ ]
대상 행 프리뷰: [ ][ ][ ][ ][ ]
```

중요한 점:

```text
실제 셀이 먼저 생긴 것처럼 보이면 안 됨
점선/반투명/희미한 결과 프리뷰가 먼저 보여야 함
```

## 4. 셀 추가와 분배 규칙

새 셀 추가를 전체 보드 기준으로 해석하면 안 된다.

구현 기준:

- 새 셀 추가는 해당 행 또는 대상 구간 기준
- 3행에 추가하면 3행만 6칸으로 균등 분배
- 다른 행은 그대로 유지
- 예를 들어 1번 줄이 4칸이고 그 줄에 셀을 추가하면 1번 줄만 5칸이 되며, 같은 줄 안의 기존 공간에서 5칸이 균등분배되어야 함
- 다른 줄은 4칸 그대로 유지되어야 함
- 셀의 좌측/우측 반을 기준으로 삽입 위치 결정
- `균등` 기준은 평균값

왼쪽 보존 규칙:

```text
현재: [0][1][2][3][4]
0,1,2 폭은 이미 세팅 완료
3 오른쪽 또는 4 왼쪽에 새 셀 삽입

결과:
0,1,2 폭은 유지
3,4,새셀 쪽만 남은 공간에서 평균 기준 균등 분배
```

실패 기준:

```text
새 셀 하나 넣었다고 행 전체가 다시 균등분배되면 실패
이미 맞춘 왼쪽 셀 폭이 풀리면 실패
다른 행까지 같이 바뀌면 실패
```

## 5. 선택과 단축키

엑셀식 조작을 가져오되, 화면에 설명 문구를 길게 달지 않는다.

필수 조작:

- `Ctrl + 클릭`: 떨어진 셀 부분 선택
- `Shift + 클릭`: 범위 선택
- `Shift + 방향키`: 선택 범위 확장
- `Shift + Ctrl + ←`: 선택 셀 왼쪽에 새 셀 추가
- `Shift + Ctrl + →`: 선택 셀 오른쪽에 새 셀 추가
- `Delete`: 선택 셀 내용 삭제
- `Ctrl + Z`: 되돌리기
- `Ctrl + Y`: 다시 실행

정렬 버튼:

- 좌/중/우 버튼은 유지
- 텍스트 `좌`, `중`, `우` 대신 엑셀처럼 직관적인 아이콘/그림으로 표현
- 모서리/위치 선택 버튼은 `상좌/상우/하좌/하우` 같은 글자 표현을 쓰지 않음
- 모서리/위치 선택은 버튼 안에 `↖`, `↗`, `↙`, `↘` 아이콘만 표시
- 툴팁이나 접근성 라벨에는 `좌상단`, `우상단`, `좌하단`, `우하단`처럼 풀어쓴 표현 사용
- 설명 문구를 UI에 길게 붙이지 않음
- `선택영역 잡기` 버튼은 제거
- 되돌리기/다시실행은 글자 버튼보다 엑셀처럼 휘어진 화살표 아이콘으로 표시
- 병합/분할 버튼은 지금보다 크게 만들고, 누르기 쉬운 주요 버튼으로 배치

## 6. 툴바, 색상, 공간 활용

현재 상단 툴바는 빈 공간이 많고, 작은 버튼이 한 줄에 눌려 있어 실제 조작이 답답해 보인다. 공간을 더 효율적으로 쓰고, 자주 쓰는 조작은 미리보기 근처에 크게 둔다.

표 만들기:

- `NxN` 텍스트는 제거
- `[] x [] 표 만들기` 형태만 남김
- 행/열 숫자 입력칸은 너무 작지 않게 유지
- 표 만들기 버튼은 입력칸 바로 옆에 둠

선택 상태 헤더:

- `1개 셀 선택됨` 텍스트는 표 안쪽 큰 제목처럼 두지 않음
- 상단 툴바/편집 헤더 쪽으로 이동
- 선택 상태는 지금보다 크게 보여주되, 작업 공간을 잡아먹지 않게 헤더 안에 정렬
- 예: `1개 셀 선택됨`, `3개 셀 선택됨`, `A1:B3 선택됨`

셀 추가/휴지통:

- 셀 추가와 휴지통은 미리보기 근처, 표 바로 위쪽에 크게 배치
- 드래그 중 바로 던질 수 있어야 하므로 작은 상단 버튼으로만 두면 안 됨
- 휴지통에는 보조 문구를 길게 달지 않음
- 삭제는 휴지통 드롭존에 놓을 때만 실행

색상 UI:

- 배경색과 글자색은 자유 팔레트보다 자주 쓰는 색상 10개 정도를 드롭다운으로 제공
- 드롭다운에는 색상 이름보다 실제 색상 스와치가 먼저 보이게 함
- 배경색/글자색을 따로 고르는 방식도 가능하고, 현장용 세트 구성을 제공해도 됨
- 추천 기본 세트는 `흰 배경/검정 글자`, `검정 배경/흰 글자`, `노랑 배경/검정 글자`, `초록 배경/흰 글자`, `빨강 배경/흰 글자`, `파랑 배경/흰 글자`, `주황 배경/검정 글자`, `회색 배경/흰 글자`, `투명 배경/검정 글자`, `투명 배경/흰 글자`
- 선택한 색상은 셀 글자 그림자와 같이 적용되어 실제 사진 위에서 읽혀야 함

공간 활용 기준:

```text
상단은 표 만들기/실행 취소/병합/분할/색상/선택 상태 중심으로 정리
미리보기 주변은 셀 추가/휴지통/드래그 작업 중심으로 정리
작업 설명 문구보다 실제 버튼과 드롭존을 크게
큰 빈 헤더나 중복 좌표 표시는 제거
```

## 7. 빠른 롱프레스와 핸들

롱프레스는 기능 실행이 아니라 빠른 표시용으로만 쓴다.

구현 기준:

- 선택된 셀에는 핸들/타겟점 표시
- 빠른 롱프레스 `250~300ms`: 핸들/타겟점 즉시 표시
- 롱프레스가 잠금/삭제/이동을 직접 실행하지 않음
- 누른 상태에서 움직임이 시작되면 롱프레스 취소
- 움직임이 시작되면 드래그 이동으로 판정

권장 판정:

```text
250~300ms 동안 거의 안 움직임 -> 핸들/타겟점 표시
3~4px 이상 움직임 -> 롱프레스 취소, 드래그 시작
```

핵심:

```text
롱프레스 = 빠른 보조 호출
롱프레스 != 잠금 실행
롱프레스 != 삭제 실행
```

## 8. 리사이즈

셀 크기 조절은 자유 조절이어야 한다.

구현 기준:

- 경계선/빨간선을 잡고 당기면 해당 행/셀 폭 조절
- 이때는 균등분배가 아니라 자율분배
- `0.25` 단위 같은 스냅 없이 자유롭게 이동
- 셀과 셀 사이 세로 경계선을 잡고 좌우로 움직이면 해당 행의 양옆 셀 폭만 늘고 줄어듦
- `Shift`를 누른 상태로 셀 사이 세로 경계선을 움직이면 같은 세로줄 전체가 함께 늘고 줄어듦
- `Shift` 리사이즈는 각 행의 기존 폭 차이에 같은 delta를 더하는 방식이 아니라, 포인터의 실제 x좌표에 맞춰 같은 세로 경계선이 일직선으로 정렬되어야 함
- 1번 줄과 2번 줄 사이 같은 가로 경계선을 잡고 움직이면 행 높이를 조절할 수 있어야 함
- 셀 사이 세로 경계선은 빨간 가이드/히트 영역으로 보여야 하며, 셀 이동 드래그와 헷갈리면 실패
- 행 사이 가로 경계선도 직접 잡을 수 있는 히트 영역이 있어야 하며, 조절 중 행 높이가 즉시 따라와야 함
- 리사이즈 드래그는 시작 시점만 히스토리에 기록하고, 움직이는 동안에는 끊김 없이 현재 화면을 갱신해야 함
- `균등` 버튼은 선택된 행/셀의 리사이즈 폭을 초기화해서 같은 행 안의 셀들이 다시 같은 폭을 가져야 함
- 편집 보드판의 행별 셀 수, 셀 폭 리사이즈, 행 높이 리사이즈는 4:3 휴대폰 미리보기에도 같은 비율로 즉시 반영되어야 함
- 모바일/PC 모두 `+` 크로스 부분을 잡아 당기면 전체 열/행 조절로 해석

실패 기준:

```text
움직임이 끊겨 보이면 실패
항상 균등분배로만 바뀌면 실패
Shift 조절과 일반 조절이 구분되지 않으면 실패
Shift로 당겼는데 행마다 세로 경계선이 서로 다른 x 위치에 있으면 실패
리사이즈 후 `균등`을 눌렀는데 셀 폭이 그대로면 실패
편집판에서는 폭/행 높이가 바뀌었는데 휴대폰 미리보기는 예전 균등 표로 보이면 실패
```

## 9. 셀 추가/삭제 드롭존

셀 추가/삭제는 미리보기 근처에 크게 둔다.

구현 기준:

- 미리보기 아래 또는 가까운 위치에 큰 드롭존 배치
- 가능하면 표 바로 위쪽, 미리보기와 가까운 곳에 배치
- 셀 추가 드롭존
- 삭제/휴지통 드롭존
- 기존 상단의 작은 셀 추가/휴지통 버튼만으로 끝내지 않음
- 표 바로 위쪽에도 큰 셀 추가 드롭존과 큰 삭제 드롭존이 있어야 하며, 상단 툴바 버튼은 보조 조작으로만 남김
- 드래그 중일 때만 강조
- 초록 경계 밖 드롭은 삭제가 아니라 취소
- 삭제는 휴지통/삭제 드롭존에 놓을 때만 실행

## 10. 잠금은 후순위

잠금은 지금 핵심 구현이 아니다. 실제 표 구성을 다 맞춘 뒤 필요한 셀만 잠그는 흐름으로 나중에 붙인다.

나중에 구현할 기준:

- 잠긴 셀은 새 셀 추가/균등분배/자동 재배치 때 폭 유지
- 잠긴 셀은 리사이즈 핸들로 움직이지 않거나 잠금 해제 후 조절
- PC는 선택/호버 상태에서 자물쇠 아이콘
- 모바일은 선택 후 아이콘 탭
- 롱프레스가 직접 잠금을 실행하지 않음

## 구현 우선순위

### 1순위

```text
선택 셀 자체 드래그
셀 추가 드래그
휴지통 드롭 삭제
미리보기 보드판 위치 이동
드래그 중 공중 분리
점선/반투명 프리뷰
초록 경계 밖 취소
휴지통 드롭만 삭제
손을 떼기 전까지 데이터 미변경
선택영역 잡기 버튼 제거
```

### 2순위

```text
휴대폰 실사용 기준 4:3 미리보기 확대
철근.png 실제 사진 배경 적용
4:3 PHOTO 제거
표 기본 배율 50%에서 시작 후 화면 기준 재조정
사진 위 가독성 보정
```

### 3순위

```text
상단 툴바 공간 재정리
NxN 제거, [] x [] 표 만들기만 유지
선택 상태를 편집 헤더로 이동
셀 추가/휴지통을 미리보기 근처에 크게 배치
정렬 아이콘화
모서리/위치 버튼은 ↖ ↗ ↙ ↘ 아이콘만 사용
되돌리기/다시실행 휘어진 화살표 아이콘화
병합/분할 버튼 확대
```

### 4순위

```text
클릭/입력 분리
셀 표기 A1/B1 방식 적용
전체 폰트 그림자 적용
행 단위 셀 추가
삽입 지점 왼쪽 폭 보존
오른쪽/대상 그룹 평균 균등 분배
Ctrl/Shift 선택
Shift+방향키 선택 확장
Shift+Ctrl+좌우 새 셀 추가
```

### 5순위

```text
경계선 자유 리사이즈
Shift 리사이즈 전체 행/열 적용
배경색/글자색 드롭다운 10개 구성
빠른 롱프레스 핸들 표시
```

### 후순위

```text
셀 잠금
잠금 아이콘
잠금 상태에서 자동 재배치 보호
```

## Sisyphus에게 전달할 한 문장

기존 1차 웹 편집기 요구사항은 구현 완료 기준으로 비우고, 이제는 `실제 4:3 현장 사진 미리보기 위에서 표가 50% 배율로 자연스럽게 보이고, 클릭은 선택만, 입력은 Enter/더블클릭, 드래그 중에는 Lucid처럼 놓기 전 결과를 점선/반투명으로 먼저 보여주며, 상단 툴바는 A1식 표기/색상 드롭다운/큰 병합·분할/휘어진 실행취소 아이콘/미리보기 근처 셀추가·휴지통 중심으로 정리한 편집 UX`를 구현한다.
